{
  "name": "online-slide-deck-template",
  "version": "0.1.0",
  "private": false,
  "description": "A standalone browser-native slide deck scaffold with animated layouts, media frames, pan/zoom, fullscreen, and keyboard navigation.",
  "scripts": {
    "start": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "lottie:setup": "npx skills add diffusionstudio/lottie",
    "lottie:showcase": "node scripts/generate-showcase-lotties.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "lottie-web": "^5.13.0"
  }
}
